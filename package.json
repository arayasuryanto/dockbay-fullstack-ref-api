{
  "name": "dockbay-fullstack-ref-api",
  "version": "1.0.0",
  "private": true,
  "description": "Dockbay full-stack reference backend: reads DATABASE_URL, proves a live Postgres connection.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}
